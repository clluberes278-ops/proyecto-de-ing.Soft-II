# ============================================================================
# CONFIGURACIÓN DE CONEXIÓN A SQL SERVER
# ============================================================================

# SQL Server
DB_SERVER=localhost
DB_USER=sa
DB_PASSWORD=Miyabi24
DB_DATABASE=UniversidadDB
DB_PORT=1433

# Node Server
NODE_ENV=development
PORT=3000
HOST=localhost

# CORS
CORS_ORIGIN=http://localhost:3000, http://127.0.0.1:5500

# Nota: Si SQL Server está en otra máquina, cambiar localhost por la IP
# Ejemplo: DB_SERVER=192.168.1.100